Since the Unix version reads differently than the windows version it actually crashes on bad input. On windows, reading for int and getting text would simply yield 0. 
I'll fix it at some point probably.
